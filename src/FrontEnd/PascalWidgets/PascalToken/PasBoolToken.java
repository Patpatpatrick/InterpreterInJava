package FrontEnd.PascalWidgets.PascalToken;

import FrontEnd.TokenType.TokenType;

public class PasBoolToken extends PasConstToken {
    public PasBoolToken(String content,TokenType type,int numOfLine,int offSet) {
        super(content,type,numOfLine,offSet);
    }
}
