package FrontEnd.PascalWidgets.PascalToken;

import FrontEnd.TokenType.TokenType;

public class PasRealToken extends PasConstToken {
    public PasRealToken(String content,TokenType type,int numOfLine,int offSet) {
        super(content,type,numOfLine,offSet);
    }
}
